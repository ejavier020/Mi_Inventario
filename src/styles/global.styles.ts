import { StyleSheet } from 'react-native';

export const globalStyles = StyleSheet.create({
  fullScanner: {
    flex: 1,
    backgroundColor: '#000',
  },
  closeBtn: {
    position: 'absolute',
    bottom: 50,
    alignSelf: 'center',
  },
});